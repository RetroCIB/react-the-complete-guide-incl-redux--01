import React, {useState} from "react";

import './Formular.css';

import Form from "./Form";

const Formular = () => {

    return (
        <div className="formular">
            <Form></Form>
        </div>
    )
}

export default Formular;